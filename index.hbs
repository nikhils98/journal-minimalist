{{!< default}}

<main id="gh-main" class="gh-main gh-outer">
    <div class="gh-inner">
        <div class="gh-wrapper">
            <section class="gh-section">
                <div class="gh-feed">
                    {{^is "paged"}}
                        {{#foreach posts}}
                            {{> loop}}
                        {{/foreach}}
                    {{else}}
                        {{#foreach posts}}
                            {{> loop}}
                        {{/foreach}}
                    {{/is}}
                </div>

                <button class="gh-loadmore gh-btn">Load more</button>
            </section>

            <aside class="gh-sidebar">
                <section class="gh-section">
                    <h2 class="gh-section-title">About</h2>

                    <div class="gh-about">
                        <section class="gh-about-wrapper">
                            {{#if @site.description}}
                                <p class="gh-about-description">{{@site.description}}</p>
                            {{/if}}
                        </section>
                    </div>

                </section>

                <div style="text-align: center">
                    <a href="https://www.linkedin.com/in/nikhil-satiani/" target="_blank">
                        <img class="custom-icon" src="assets/icons/linkedin.svg" alt="linkedin">
                    </a>
                    <a href="https://github.com/nikhils98" target="_blank">
                        <img class="custom-icon" src="assets/icons/github.svg" alt="github">
                    </a>
                    <a href="https://stackoverflow.com/users/6932746/long-hair-programmer" target="_blank">
                        <img class="custom-icon" src="assets/icons/stack-overflow.svg" alt="stackoverflow">
                    </a>
                </div>

                {{#get "posts" filter="featured:true" limit="all" as |featured|}}
                    {{#if featured}}
                        <section class="gh-section">
                            <h3 class="gh-section-title">Featured</h3>

                            <div class="gh-featured gh-feed">
                                {{#foreach featured}}
                                    {{> loop}}
                                {{/foreach}}
                            </div>
                        </section>
                    {{/if}}
                {{/get}}

                {{#get "tags" include="count.posts" limit="all" as |topic|}}
                    <section class="gh-section">
                        <h3 class="gh-section-title">Topics</h3>

                        <div class="gh-topic">
                            {{#foreach topic}}
                                <a class="gh-topic-item" href="{{url}}">
                                    <h3 class="gh-topic-name">{{name}}</h3>
                                    <span class="gh-topic-count">
                                        {{plural count.posts empty="0 issues" singular="% issue" plural="% issues"}}
                                    </span>
                                </a>
                            {{/foreach}}
                        </div>
                    </section>
                {{/get}}
            </aside>
        </div>
    </div>
</main>